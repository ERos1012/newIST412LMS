
@Entity
public class User {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true)
    private String username;

    private String hashedPassword; // Store hashed passwords

    // Other user-related fields (e.g., email, roles)

    // Getters and setters
}
